# Project Rules

- **Package Manager**: Always use `bun` (and `bunx`) for package management, script execution, running dev servers, and running commands in this project (e.g., `bun dev`, `bun install`, `bunx`). Do not use `npm`, `npx`, `yarn`, or `pnpm`.
